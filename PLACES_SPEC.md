# ayoo «Места» — discovery + отзывы (retention feature)

Yelp-слой внутри ayoo: поиск мест, агрегированные отзывы, фильтры, значки
партнёр/оффер. Лояльность — крючок: discovery → визит → скан → баллы →
питомец растёт → возврат. Строим **поверх существующего** (Venue/Review/Offer,
импорт-пайплайн `venue-imports.ts`, карта), не с нуля.

## Решения (зафиксировано)
- Сид базы: **OSM (Overpass) + Google Places по требованию**.
- Отзывы: **нативные ayoo (полные) + Google рейтинг отдельным чипом** со ссылкой.
- **Баллы за отзыв** — да, с гейтом «подтверждённый визит» и анти-фродом.
- Города на старте: Белград + Нови-Сад.

---

## 1. Данные: трёхслойная модель

**Слой 1 — граф мест (OSM, бесплатно, ODbL+атрибуция).**
Overpass: `amenity=cafe|bar|restaurant|fast_food`, `shop=*`, beauty/fitness/yoga
(см. маппинг ниже). Тысячи мест в первый день. Хранить можно.

**Слой 2 — обогащение (Google, on-demand, кэш в БД).**
При открытии карточки, если `lastEnrichedAt` пусто/старше TTL → Google Place
Details → рейтинг, число отзывов, фото, часы, ценовой уровень → апдейт строки.
Google ToS: `place_id` хранить вечно можно; рейтинг/фото — кратко с атрибуцией
и ссылкой; **тексты чужих отзывов не храним**.

**Слой 3 — твоё (realtime).** Партнёр, офферы, ставка баллов, нативные отзывы,
свои фото.

## 2. Где храним и как обновляем
Единый источник — **Postgres (Supabase)**, таблица `Venue`. Внешние данные лежат
в той же строке, `lastEnrichedAt` = TTL. Отдельного стора под места нет.

Три механизма записи (все → в ту же Postgres):
1. **Cron `venues-sync`** (рядом с другими крон-роутами, защита `_verify.ts`):
   раз в неделю Overpass → `normalizeImportVenue → findExistingVenue → upsert` →
   лог в `VenueImportLog`. Триггер: QStash-расписание / Vercel Cron.
2. **Lazy enrich (on-view)** в `venue.detail`: stale → Google → апдейт строки.
3. **Realtime** — партнёр/оффер/отзыв из приложения.

```
OSM (weekly cron) ─┐
Google (on-view)  ─┼─► Supabase Postgres (Venue, lastEnrichedAt=TTL) ─► app
app writes        ─┘
```

Стоимость: OSM бесплатно; Google только на просмотренные места + TTL →
расход не растёт от размера базы.

## 3. Категории (enum `VenueCategory`)
Добавляем `BEAUTY`, `FITNESS`, `YOGA` (магазины = существующий `RETAIL`).

| Категория | Enum | OSM-теги |
|---|---|---|
| Кафе/бар | `CAFE` | `amenity=cafe\|bar\|pub` |
| Ресторан | `RESTAURANT` | `amenity=restaurant\|fast_food` |
| Магазины | `RETAIL` | `shop=*` (кроме beauty ниже) |
| Салоны красоты | `BEAUTY` | `shop=hairdresser\|beauty\|nail_salon\|cosmetics`, `leisure=spa` |
| Фитнес | `FITNESS` | `leisure=fitness_centre\|sports_centre`, `amenity=gym` |
| Йога | `YOGA` | `leisure=fitness_centre`+`sport=yoga` |
| Прочее | `SERVICE`/`OTHER` | fallback |

OSM для йоги/бьюти местами разрежён → опционально догон Google Nearby Search
по категории при сидинге.

## 4. Экран «Места» (discovery)
Карта + список (есть). Расширить инпуты `venue.search`/`venue.nearby`:
`category, openNow, partnerOnly, hasOffer, minRating, sort(near|rating|rate)`.
Значки в карточке/на пине: ⭐ партнёр (`isPartner`) · 🎁 оффер (активный `Offer`)
· 💰 ставка баллов (`pointsPerCurrency`/boost).

## 5. Карточка места
Фото → агрегат: **ayoo-отзывы (полные, с фото) + Google-чип** «4.6 ★ · 320 на
Google →» (атрибуция/ссылка). Оффер, ставка баллов, часы, маршрут, кнопка
«оставить отзыв» (если визит подтверждён).

## 6. Отзывы + баллы (анти-фрод)
- **Гейт «подтверждённый визит»**: баллы за отзыв только если в этом venue была
  транзакция/скан/чек-ин у пользователя.
- Один отзыв на место (`@@unique([userId, venueId])`); баллы — разово при первом
  (`Review.pointsAwarded`), правка не доначисляет.
- Условия: рейтинг + текст ≥ ~20 симв.; бонус за фото. Дневной кап.
- Модерация: скрытие по порогу жалоб + фильтр мата.
- Сумма: фикс ~20–30 баллов (подобрать).

## 7. Ретеншн-петля
Скан в месте → пуш «Как тебе X? Оставь отзыв (+N)» → отзыв → баллы → питомец
растёт. «Новый оффер рядом». Бейдж «местный гид» / косметика питомца за N
отзывов.

## 8. Дельта модели
`Venue`: `osmId String?`, `priceLevel Int?`, `lastEnrichedAt DateTime?`.
`Review`: `photos String[]`, `helpfulCount Int @default(0)`, `pointsAwarded Boolean @default(false)`.
`enum VenueCategory`: + `BEAUTY`, `FITNESS`, `YOGA`.

## 9. Этапы
- **P1** OSM-импортёр + cron `venues-sync` + миграция модели → база наполнена.
- **P2** Экран «Места»: фильтры, значки, карта/список.
- **P3** Lazy-обогащение Google + карточка с агрегатом отзывов.
- **P4** Отзывы + баллы + пуш-петля «оставь отзыв».
- **P5** Cron-свежесть (ресинк + рефреш активных) + дашборд импорта.

## 10. Статус
- [x] Концепт + архитектура данных (этот файл)
- [x] **P1** — импортёр OSM + cron + миграция → **10 408 мест залито** (BG 8330 · NS 2076), миграция в проде
- [x] **P1-инфра** — API задеплоен, `venue.discover` живой, крон `venues-sync` в vercel.json (Пн 03:00)
- [x] **P2** — экран «Места» (поиск, чипы категорий, фильтры партнёр/оффер, значки, дистанция, бесконечная прокрутка, тоггл карты) — в проде
- [ ] **P3** — lazy-обогащение Google (рейтинг/фото/часы) + карточка места с агрегатом отзывов
- [ ] **P4** — отзывы + баллы + пуш-петля
- [ ] **P5** — рефреш активных мест + дашборд импорта
